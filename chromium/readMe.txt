Chromium is required by selenium. To install Chromium on a linux machine, follow these steps on the project root level:

1. Make the script executable by the following command:
    chmod +x ./chromium/chromium-downloader.sh

2. Execute the script:
    ./chromium/chromium-downloader.sh

3. Chromium will be installed and now pdf can be generated via puppeteer.